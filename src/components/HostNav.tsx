// Host portal nav is intentionally empty for now: the Guest/Host switch already
// lands on My Spaces, so a single-item pill here would just duplicate it. Add
// links here once there's more than one host page to navigate between.
export const HostNav = () => null;
